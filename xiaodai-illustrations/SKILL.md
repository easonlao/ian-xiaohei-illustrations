---
name: xiaodai-illustrations
description: 提供统一的小呆视觉系统底座。用于用户要求围绕"小呆""手绘""怪诞""纯白""隐喻配图""文章插图""图文卡片""视频流封面""配图建议""shot list""场景判断""去标题/改图"等任务时，输出角色定义、视觉 DNA、隐喻方法与底层提示词约束；具体画幅与场景由后续模式层决定。
---

# 小呆视觉系统 Layer 0

## 核心定位

当前 skill 的职责不是绑定某个固定画幅或平台，而是提供统一的小呆角色、统一视觉语言和统一隐喻方法，作为后续不同场景配图层的共用底座。

这一层先解决：

- 小呆是谁
- 小呆不能变成什么
- 画面应该是什么气质
- 如何把抽象内容翻译成物理动作和低科技隐喻

这一层暂时不解决：

- 具体是长文横图、图文卡片还是竖屏封面
- 固定的画幅比例
- 平台专用的信息密度和标题策略

默认视觉 IP 是“小呆”（小火人形态）：纯手绘亮红色火焰形状实体剪影小人，脸上有一对白色小圆点眼睛，四肢为纤细的黑色火柴人线条。小呆必须参与画面的核心动作，不能只是站在旁边当装饰。

## 先读这些参考

按任务需要读取，不要一次塞满上下文：

- `references/style-dna.md`：统一视觉语言、颜色、留白与非目标风格。
- `references/user-ip.md`：小呆 IP 本体、角色边界与参考图绑定。
- `references/composition-patterns.md`：把抽象内容转成物理动作和低科技隐喻的方法。
- `references/prompt-template.md`：layer 0 通用提示词模板，等待上层场景传入画幅和场景参数。
- `references/qa-checklist.md`：生成后检查和迭代规则。
- `assets/examples/`：只作低频视觉校准，不进入默认生成路径。不要照抄这些案例的构图、物件或标注。

## 工作流

### 1. 识别场景，不先锁画幅

先读用户给的正文、链接、Notion 页面、Markdown 文件或截图内容。提炼：

- 核心观点是什么
- 内容主要面向什么消费场景
- 这个场景更偏横图、卡片还是竖屏
- 哪些内容适合用图解释
- 哪些地方只适合文字，不需要图

如果用户已经指定场景或比例，直接沿用。  
如果用户没有指定，不要静默锁成 `16:9`；先判断更适合什么场景，再继续往下生成方案。

### 2. 先做场景判断，再输出 layer 0 约束

如果用户只给一个主题、一句话观点或一段内容，而没有给完整的 scene spec，不要假设用户会自己补齐。你需要先自行判断，再把判断结果显式说出来。

优先输出一个很短的 scene decision：

- `scene_mode`
- `format_goal`
- `canvas_ratio`
- `subject_density`
- `structure_type`
- `core_metaphor`

scene decision 的语气要像“这次任务的判断结果”，不是“这个系统永远只能这样”。

例如可以说：

- 这次更适合 `article_inline`
- 推荐 `16:9 horizontal`
- 主体密度偏低，保留大留白
- 结构类型是 `conceptual_metaphor`
- 核心隐喻是“小呆被自己的线团缠住，不是不想动，而是动不了”

字段边界要清楚：

- `scene_mode`：内容要落在哪种消费场景里，例如 `article_inline`、`social_single`、`feed_cover`、`vertical_cover`
- `structure_type`：这张图用什么表达结构，例如 `conceptual_metaphor`、`workflow`、`before_after`、`map_route`
- `format_goal`：这一场景下的阅读目标与构图目标，例如“横向呼吸感段落插图”或“手机信息流里一眼停住的竖屏封面”

`scene_mode` 不要写成 `conceptual_metaphor`、`workflow` 这类结构词。那是 `structure_type`，不是场景。

优先从下面这些 `scene_mode` 里选：

- `article_inline`：长文正文中的段落配图
- `social_single`：单张观点配图，可用于文章内嵌或社交传播
- `feed_cover`：信息流封面，强调一眼停住
- `vertical_cover`：竖屏封面，强调手机首屏张力

如果确实不在这 4 类里，也要先回答“这张图被放在哪里被看见”，再决定 scene_mode，而不是退回结构词。

如果同一个主题要跨两个场景输出，先固定核心隐喻，再分别调整：

- 视角远近
- 留白比例
- 信息密度
- 标注数量
- 画面张力

如果用户只是说“怎么配图 / 这个 IP 怎么用于某种内容 / 先出策略”，先输出：

- 推荐场景
- 推荐画幅
- 推荐主体占比
- 推荐文字密度
- 小呆在图里承担什么动作
- 建议使用什么低科技隐喻

如果任务明确是长文配图，再继续输出 shot list。每张图写清楚：

- 图的主题
- 核心意思
- 结构类型
- 小呆在图里做什么
- 建议元素
- 建议中文标注词

### 3. 生成时使用场景参数，而不是默认 16:9

如果用户明确要求"生成 / 输出 / 做图 / 帮我生成"，不要停下来等确认；用内置 `image_gen` 每张单独生成。不要把多张图拼在一张里。

每张图只讲一个核心结构。**生图时必须以 `references/user-ip.md` 中的标准参考图为参考图（使用 ref_images 参数），确保 IP 特征一致。**

如果用户没有给完整 scene spec，生成前先用 3-5 行文字显式输出你本次采用的：

- `scene_mode`
- `format_goal`
- `canvas_ratio`
- `subject_density`
- `structure_type`
- `core_metaphor`

然后再写最终 prompt。不要直接从一句主题跳到一大段 prompt，中间没有任何场景判断。

提示词必须包含：

- 用户指定或当前判断出的场景说明
- 用户指定或当前判断出的画幅说明
- 用户指定或当前判断出的主体密度
- 纯白背景
- 黑色手绘线稿
- 少量红色/橙色/蓝色中文手写批注
- 大量留白
- 小呆作为核心动作主体
- 禁止 PPT、商业插画、幼稚可爱、复杂架构、左上角类型标题

不要复刻过往案例。案例只提供风格密度和小呆参与方式，不能直接复用"传送带断点 / 小呆拉线 / 素材鱼 / 盖章工具箱 / 常见坑路径"等已有构图，除非用户明确要求复刻某张图。每次都要从当前文章重新发明一个奇怪但成立的隐喻。

特别注意：

- `16:9` 可以是这一次长文场景的正确结论，但不能写成系统默认真理。
- `3:4` 或其他竖屏比例可以是封面场景的正确结论，但不能反过来写成所有信息流都必须如此。
- layer 0 负责统一 IP、风格和隐喻，不负责把某个场景包装成唯一正确答案。

### 4. 检查与迭代

生成后检查 `references/qa-checklist.md`。如果出现以下问题，优先重生成或局部编辑：

- 小呆只是装饰
- 画面太满
- 太像流程图/PPT
- 中文太多或错字严重
- 左上角出现"常见坑/流程图/系统架构图"等标题
- 画风太可爱、幼稚、死板
- 背景不是干净白底

### 5. 保存交付

如果用户在 workspace 内工作，把最终图复制到：

```text
outputs/<article-slug>-illustrations/
```

按顺序命名：

```text
01-topic-name.png
02-topic-name.png
```

保留原始生成文件，不要覆盖已有输出，除非用户明确要求替换。

## 输出口径

生成前的策略输出要短而准。生成后的交付要包含：

- 使用了什么场景判断
- 使用了什么画幅判断
- 生成了几张
- 每张图的用途
- 保存路径
- 哪些图最稳，哪些图是可选

不要长篇解释风格理论；让图自己说话。
